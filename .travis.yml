language: c
os: linux
services:
    - docker
before_install:
    - sudo apt-get -y install meson
    - sudo apt-get -y install libwayland-dev
    - sudo apt-get -y install julia
script:
    - mkdir build
    - meson build
    - cd build
    - meson compile
    - meson install
