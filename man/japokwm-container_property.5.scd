japokwm-property(5)

# Access
	can only be accessed via a container object
# Description
	The property of a container object.
# Functions
# Methods
# Variables
	bool floating = False
		True if the container is floating, False if not.
	geom geom = Geom.new(x, y, width, height)
		The geometry of the container.
